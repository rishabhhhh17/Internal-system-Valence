VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
VITE_GEMINI_API_KEY=
# Fathom — server-side key (NOT prefixed VITE_). Goes through the Vercel
# function at /api/fathom so CORS is handled and the key stays out of
# the browser bundle. Paste your personal API token from Fathom →
# Settings → API Access.
FATHOM_API_KEY=
